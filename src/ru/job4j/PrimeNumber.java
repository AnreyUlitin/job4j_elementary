package ru.job4j.ru.job4j.loop;

public class PrimeNumber<CheckPrimeNumber> {

    private static int CheckPrimeNumber;

        (int finish)

    public PrimeNumber() {
        int count = 0;
        for (int number = 1; count < finish; count++) {
            if (PrimeNumber.CheckPrimeNumber.check(number)) {

            }
        }
        return count;
    }
}
